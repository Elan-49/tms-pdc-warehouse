TSKK FIX V4.5.77

Root causes fixed from console errors:
1. "Cannot access 'saveTSKKDraft' before initialization"
   The event listeners were binding saveTSKKDraft before the const had been initialized.
   Fix: initialize syncInputs -> captureTSKKDraft -> saveTSKKDraft first, then bind listeners.

2. "captureTSKKDraft is not defined"
   registerDraftCapture was outside the lexical scope where captureTSKKDraft was declared.
   Fix: register the active TSKK draft capture inside open(), after captureTSKKDraft exists.

No changes to calculation formulas, Supabase schema/query logic, authentication, or CSS.

Validation performed:
- node --check app.js: PASS
- archive integrity: PASS
